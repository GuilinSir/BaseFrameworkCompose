plugins {
    id("cn.guilin.framework.convention.android.library")
    id("cn.guilin.framework.convention.android.compose")
}

android {

    buildFeatures {
        // 启用BuildConfig生成
        buildConfig = true
    }
}

dependencies {
    
}